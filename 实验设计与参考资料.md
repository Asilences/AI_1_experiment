# 基于 Wikidata 电影知识图谱的问答与答案生成实验设计

建议采用电影领域，构建固定的 Wikidata 子图，完成“实体识别与链接、关系检测、事实查询、模板回答”的管道系统，以及“问题与子图输入、Transformer 生成完整答案”的神经网络系统。用同一组测试问题比较事实准确性、文本质量、鲁棒性和计算开销，再通过学习率对照与嵌入消融解释实验现象。

本文是实施前设计，编制日期为 2026 年 9 月 12 日。以下数据规模、超参数和工作量均为计划值，尚未下载实验数据、配置训练环境或运行模型，不代表实验结果。最终报告必须用实际统计、日志、截图和指标替换计划内容。

## 一 课程要求与设计对应

已阅读本地《人工智能I_实验报告要求.docx》和《人工智能I-课程实验.pdf》全部内容，并核对 PDF 两页。用户给出的 Word 路径在实际目录中对应文件名“人工智能I_实验报告要求.docx”。

| 原始要求 | 本设计的落实方式 |
| --- | --- |
| 使用 Wikidata 并限定领域 | 抽取电影及导演、类型、国家、语言等相关实体 |
| 构建子图并进行知识图谱嵌入 | 用 TransE 学习实体、关系向量，并在神经生成模型中实际使用 |
| 管道式方法 | 词典式实体识别与链接、关系分类器、图查询、完整句模板 |
| 端到端方法 | 输入问题与相关子图，微调小型编码器—解码器 Transformer 生成答案 |
| 比较两种方法 | 相同测试集、知识来源、实体链接入口和计时条件；分别评估准确性、鲁棒性、开发复杂度 |
| BLEU、ROUGE 与人工评价 | 自动文本指标、实体集合指标、人工准确性／完整性／流畅度评分 |
| 1 张终端环境截图 | 展示实际 Python 路径和版本、主要依赖版本、项目目录及硬件信息 |
| 不少于 2 张运行截图 | 数据构建、训练中间过程、完整评估过程至少各保留 1 张 |
| 基础实验后修改重要参数，重跑至少 3 次 | 保留基础学习率，另设 3 个学习率，均从同一初始模型重新训练 |
| 数值结果与时间或开销 | 记录指标、训练时长、平均及 P95 单题延迟、可测的峰值显存 |
| 至少 1—2 张参数对比图 | 四种学习率的验证损失曲线，以及事实指标／ROUGE-L 对比图 |
| 可运行代码与报告同时提交，代码包不含完整数据 | 提供获取及处理脚本、查询文件、配置、目录说明和数据校验信息 |

PDF 第 1 页分别描述两种模型，第 2 页又明确要求比较二者，因此建议实际实现两种方法。报告要求中的“参数修改后至少 3 次”按保守方式执行为“基础 1 次＋3 种修改设置”，而不是总共只跑 3 次。

## 二 研究问题和范围

本实验回答三个问题：管道式与神经生成式系统在什么条件下各有优势；生成模型学习率如何影响事实准确性和收敛；在已有三元组文本的基础上，增加 TransE 向量是否提供额外帮助。

主要任务限定为电影的单跳事实问答，输出完整句子。基础版不开展多跳推理、开放域聊天和实时联网检索。训练、验证和测试均访问预先冻结的本地知识库，以便复现和计时。

建议优先采用英文问题与英文答案、中文实验报告：Wikidata 英文标签较便于配合 `google/flan-t5-small` 的小规模实验，模型卡标示其规模约为 77M 参数。[5] 课程文件未规定问答语言。若实施时确定使用中文，统一改用中文标签、问题、参考答案和中文评测分词，并优先试验 `google/mt5-small`；该模型支持中文，但需要下游微调。[10] 两种语言的结果不可直接混在同一排行榜中。

演示用输入可形如 `Who directed <film title>?`，输出为 `<film title> was directed by <director names>.`。这里的占位符不代表已核实的真实电影事实，正式演示必须从实际下载的三元组生成。

## 三 数据获取与构建

### 1 领域和关系

计划抽取约 1000 部电影，尽量覆盖多个年代和国家，不只取少数最著名作品。电影实体通过 `instance of`（P31）及必要的电影子类关系筛选；先构建候选池，再用固定随机种子抽样并保存电影 QID 清单。

| 问答关系 | Wikidata 属性 | 问题形式 | 答案类型 |
| --- | --- | --- | --- |
| 导演 | P57 | 谁执导了这部电影 | 人物实体集合 |
| 类型 | P136 | 这部电影属于哪些类型 | 类型实体集合 |
| 制作国家 | P495 | 这部电影来自哪些国家 | 国家实体集合 |
| 原始语言 | P364 | 这部电影的原始语言是什么 | 语言实体集合 |

发行日期等字面量属性留作扩展，避免基础版混合处理日期精度、地区限定和实体嵌入。演员列表通常较长，也不作为首轮问答关系。

### 2 获取方式和可复现性

使用 Wikidata 官方 SPARQL 服务筛选电影及指定属性，必要时通过实体数据接口补齐标签、别名和修订号。官方说明支持小范围查询和单实体访问，不必下载全量数据库；结构化数据采用 CC0 许可。[1][2]

按关系分别批量提取 `(head_id, relation_id, tail_id)`，避免一次把导演、国家、类型、语言全部展开造成笛卡尔积。保存去重后的三元组、实体标签、别名、抽样清单、查询文本、提取时间和文件 SHA-256。支持分页、缓存、断点续取及失败日志；出现限流时遵循服务的 `Retry-After`。[2]

基础图使用 best-rank 的有效声明，记录采用的 truthy 语义。它不保留限定符和引用，因此基础问题不涉及“某地区首映日期”等依赖限定符的事实。[1] 提取过程可能跨多个请求，须保存实际抓取时间范围；不能把它说成数据库严格同一时刻的事务快照。

构图时保留完整多值关系，拒绝从多个导演中任意取一个作为唯一真值。问题中的“哪些”以当前冻结子图中该关系的全部有效值为标准，报告注明知识库可能不完整，不等同于对现实世界的穷尽证明。

### 3 规模和质量门槛

计划选择至少在上述 4 种关系中有 3 种可用的电影。适合基础问答的电影—关系组合须具有可读标签，且答案实体数为 1—3；超过 3 个答案的组合保留在图中，但不进入基础问答集，单独统计排除数量。

以约 1000 部电影、每部 3—4 个有效关系、每个关系 3 种表达估算，目标为约 9000—12000 个问答样本。可补充相关实体的类型等辅助边，整体子图预计为数千至数万条三元组；这是规模预算，最终以脚本统计为准。如数据不足，按预设筛选规则扩大候选池或如实缩小规模，不复制问题凑数。

清洗包括三元组去重、缺失标签处理、非法或过时声明过滤、别名冲突统计、问句去重。保存 `qid`、`relation_id`、`answer_ids`、`question`、`reference_answers`、`template_family`、`split`、`source_revision` 等字段；修订号只有实际取得时才填写。

### 4 问答标注和划分

先以电影 QID 分组，按 70%／15%／15% 划分训练、验证、测试电影，再在各组内构造问答。同一电影的全部关系问题、同义改写、别名版本和参考答案必须留在同一组。固定划分种子为 42，保存清单并检查交集为空。关系分布尽量平衡；不能为了平衡把同一电影拆开。

若最终达到 1000 部电影，则计划对应 700／150／150 部；问答条数按清洗后实际数量计算，不能机械填写 7000／1500／1500。

训练问句可以通过人工编写的多种模板扩展，但测试集应加入未用于训练的表达。为独立检验语言泛化，另构建人工改写鲁棒性集，冻结后不得据此继续调整词典、模板或模型。每个测试问题准备 2 个语义等价的完整句参考答案，至少对验证与测试参考答案进行人工核对。不同写法不能直接照抄管道系统唯一输出模板，以免 BLEU 偏向模板法。

需要区分两种数据：问答监督样本按电影划分；知识库是训练前冻结、允许检索的公共事实来源。TransE 可以在此固定图上训练，包括问答测试电影的图结构，但不能读取测试问题或参考答案。这属于“已知知识库中的新问题”设定，不证明对未知实体或缺失事实的归纳能力，也不用于宣称链路预测成绩。如果增加 MRR／Hits@K，必须另设三元组训练／验证／测试划分，不能对 TransE 已见边报告所谓测试性能。

## 四 两种系统的实现

### 1 共同入口与知识范围

用电影名称、别名和规范化规则做领域词典式命名实体识别，并将文本片段链接到 QID。先做最长精确匹配，再做受限模糊匹配；同名电影结合问句中的年份等信息消歧，无法确定则返回澄清或未识别状态，不使用测试集标注的 QID 代替预测。词典式识别是基础 NER 实现，报告中如实说明，不声称训练了通用 NER 网络。

两种系统共享同一实体链接器和冻结子图。根据预测电影提取单跳子图，最多保留 24 条事实；采用固定关系轮转和 QID 排序，不能根据测试真值挑选“正确”边。若输入长度不足，按同一规则整条移除三元组，两种方法使用相同的最终可见事实集合。记录答案证据覆盖率，以识别实体链接错误或截断造成的失败。

### 2 方法 A 管道式系统

流程为“问题 → 实体识别和链接 → 关系检测 → 子图查询 → 完整句模板”。

关系检测使用词／字符 TF-IDF 加逻辑回归，在训练问句上拟合，仅通过验证集选择阈值；将电影名片段替换为统一占位符，降低分类器记忆片名的机会。输出 4 种关系之一，低置信度时拒答。查询对应的全部答案实体后，处理单复数、连接词和实体标签，输出完整自然语言句子。

记录实体链接准确率、关系分类准确率和混淆矩阵、查询命中率、最终回答指标。每题日志包含识别实体、预测关系、命中三元组和输出，便于解释错误传播。

### 3 TransE 知识图谱嵌入

TransE 使真实三元组的实体向量与关系向量近似满足 `h + r ≈ t`，通过真实边和负采样边的损失学习结构表示。[3] 使用 PyKEEN 实现，避免重复搭建训练与采样框架。[4]

| 参数 | 基础计划 |
| --- | --- |
| 实体与关系维度 | 100 |
| 距离范数 | L1 |
| 损失 | Margin ranking loss，margin = 1 |
| 优化器 | Adam |
| 学习率 | 0.001 |
| Batch size | 256 |
| Epoch | 100 |
| 每个正例的负样本数 | 5 |
| 随机种子 | 42 |

过滤负采样中已存在于固定图的真实边，保存 loss 曲线、向量文件、ID 映射和训练时间。训练完成后冻结嵌入，以便各学习率实验复用同一份结构表示。TransE 不直接理解问题文本，其作用是补充图结构；问题语言与结构之间仍需通过下游网络学习联系。

### 4 方法 B 子图条件神经生成系统

使用 `google/flan-t5-small` 作为基础模型。输入由问题和子图事实文本组成，例如 `question: ... facts: <film> | director | <person>; ...`，直接训练生成完整答案，不在生成之后用模板替换预测实体。

为了让图嵌入真正参与回答，对每条输入三元组拼接冻结的 TransE 向量 `[h; r; t]`，经可训练投影层和归一化映射到生成模型的隐藏维度，作为结构前缀与问题、三元组的文本向量一起输入编码器。结构前缀和文本事实保持一一对应，使用 attention mask 屏蔽补齐位置。ID 向量提供结构，文本标签保留生成实体名称所需的信息。训练目标为答案 token 的交叉熵，投影层与生成模型一起更新。

这里的“端到端”遵循课程描述，指从“问题＋相关子图”到答案的神经生成；实体链接、候选子图提取和 TransE 预训练是固定前处理，不声称检索和生成实现了联合端到端优化。论文中“RDF 三元组到文本”的 WebNLG 任务可供序列化和评测设计参考，但其数据不替代本实验要求的 Wikidata。[6]

| 参数 | 基础计划 |
| --- | --- |
| 生成模型 | google/flan-t5-small，实施时固定具体 revision |
| 优化器 | AdamW |
| 学习率 | 0.0001 |
| Epoch | 5 |
| 单设备 Batch size | 8 |
| 梯度累积 | 2，有效 Batch size = 16 |
| 文本输入长度上限 | 384 tokens，额外结构前缀最多 24 个 |
| 最大生成长度 | 96 tokens |
| Weight decay | 0.01 |
| Warmup ratio | 0.1 |
| 梯度裁剪 | 1.0 |
| 解码 | 贪心，num_beams = 1，do_sample = false |
| 划分／训练种子 | 均为 42，分别记录 |

固定训练 5 个 epoch，每个 epoch 评估验证集。使用验证集答案实体集合 F1 选择 checkpoint，并预先规定平分时优先选验证损失更低者、再选更早 epoch。测试集只在配置和选模规则冻结后评估，不用来挑学习率或 checkpoint。

先做小规模训练与显存预检再冻结正式配置。若显存不足，可将单设备 batch 改为 4、梯度累积改为 4，保持有效 batch 为 16；正式对比必须对所有设置一致调整后重新开始。精度类型根据实际硬件选择并统一记录，不预先假定 GPU 支持某种混合精度。

## 五 实验矩阵

| 编号 | 系统 | 改动 | 目的 |
| --- | --- | --- | --- |
| A0 | 管道式模板系统 | TF-IDF＋逻辑回归＋图查询 | 方法对比基线 |
| B0 | 神经生成＋TransE 前缀 | 学习率 1e-4 | 神经基础实验 |
| B1 | 同 B0 | 学习率 3e-5 | 参数修改第 1 次 |
| B2 | 同 B0 | 学习率 3e-4 | 参数修改第 2 次 |
| B3 | 同 B0 | 学习率 1e-3 | 参数修改第 3 次 |
| C0 | 神经生成，无 TransE 前缀 | 学习率与 B0 相同，事实文本完全相同 | 判断嵌入的额外价值 |

B0—B3 每次均从同一个预训练 checkpoint 及相同投影初始化种子重新开始，保持数据、批次顺序、训练轮数、优化器其他参数、嵌入和解码方式一致。不能从 B0 已训练模型继续训练 B1—B3 来冒充独立对照。每组都保存各 epoch 日志及按验证集选出的最终 checkpoint。

C0 是增加结构前缀这一整项设计的消融，结果同时包含前缀容量和结构信息的影响。若要更有力地归因于“学习到的知识图谱结构”，增加相同形状和参数量、但使用随机冻结实体／关系向量的 C1 对照。还可增加仅输入问题的 C2，观察预训练记忆或训练集捷径的影响。C1、C2 为扩展，不挤占课程要求的四次学习率实验。

资源允许时，对 B0—B3 和 C0 使用训练种子 42、43、44 重复，保持数据划分种子固定，报告均值和标准差。课程最低版只有单种子时，明确结论为观察到的趋势，不宣称统计显著或普遍最优。

## 六 评价与鲁棒性

### 1 自动指标

| 指标 | 计算与解释 |
| --- | --- |
| 答案实体集合准确率 | 预测答案实体集合与标准集合完全相同的题目比例；是集合匹配，不是整句字符串匹配 |
| 答案实体集合 F1 | 每题计算集合 precision、recall、F1 后做 macro 平均，另按关系报告 |
| BLEU | 使用 SacreBLEU 计算语料级 BLEU，英文 tokenizer = 13a，保留版本和完整 signature，0—100 |
| ROUGE-L F1 | 固定分词与归一化规则；每题对两份参考取最大值后平均，报告时乘 100 |
| 证据覆盖率 | 进入最终输入的事实是否包含该题完整答案；全流程包含实体链接失败样本 |
| 运行成本 | 训练总时长、平均／P95 单题延迟、模型大小和可测的峰值显存 |

答案实体识别需从生成句子中提取答案位置的名称，按固定别名表映射 QID；不把问句主体电影计入答案。映射失败的疑似答案片段作为未匹配预测项处理，不能直接删除以抬高 precision。歧义名称进入人工复核。对有答案题，空输出 F1 记 0；无答案题独立评价，避免空集合规则掩盖问题。

SacreBLEU 用两份参考答案统一计算语料级分数。[7] ROUGE 来源于自动摘要评价，度量文本重叠而非事实真伪。[8] “导演名字正确但说成主演”的输出可能仍有很高实体 F1，因此自动实体匹配也不能单独证明事实正确，必须结合下面的人工语义核查。

延迟在相同设备、batch = 1、相同测试集下测量，排除网络下载和一次性模型加载；先固定数量预热，GPU 计时前后同步。记录是否包含实体链接、子图构建和解码，建议完整包含。第一次加载时间另列，不混入每题延迟。

### 2 人工评价

从固定测试集按关系分层抽取 100 题。对 A0 和由验证集选出的 B 系统使用同样题目，隐藏方法名称并随机排列答案。理想情况下由 2 人独立评分；若只能本人评估，如实写“单人评价”。

每题评分：准确性 0／1／2（错误或无据、部分正确、全部正确）；完整性 0／1／2（缺失、部分覆盖、全部覆盖）；流畅度 1—5。另记录是否输出了当前子图不支持的断言，并展示代表性错误。两位评审的分歧先保留原始记录，再讨论；不得假造第二评审或评分一致性。

### 3 鲁棒性集合

固定选择 100 道干净测试题，构造一一配对的 3 类版本，每类 100 题：未见表达的同义改写、合法别名替换、轻微拼写与格式噪声。三类分别比较相对对应干净问题的指标下降；若某题没有合法别名，按预定规则补抽可用题并固定清单。

再独立构造 50 道相对于当前知识库不可回答的问题，如库外电影或不存在的属性记录。分别报告拒答正确率与错误作答率；对有答案的干净题同时报告误拒答率。训练／验证阶段需包含来自其各自电影分组的无答案样例，数量建议约占原有样本的 10%，不与测试题重合，计入实际数据规模。神经模型学习明确的无依据回答，管道系统使用验证集确定阈值。阈值在测试前冻结。

鲁棒性实验和基础学习率实验分表报告，避免同时改动噪声与学习率。开发复杂度记录实际模块数、关键依赖、实现和调试工时、典型故障及维护点；不以“代码行数少”直接推断方法更简单。

## 七 图表与运行证据

建议最终保留以下图表，并全部使用实测数值。

1. 图 1：TransE 训练 loss，说明图嵌入训练是否稳定。
2. 图 2：B0—B3 验证 loss 随 epoch 的四条曲线，体现收敛差异。
3. 图 3：四种学习率对应的答案实体 F1 和 ROUGE-L，用两个子图避免量纲误读。
4. 图 4：A0 与选定 B 系统在干净／改写／别名／噪声问题上的事实指标对比。
5. 图 5：B0 与 C0 的嵌入消融对比；若加入 C1，一起展示。
6. 表 1：两种方法的准确率、F1、BLEU、ROUGE-L、人工评分、延迟和训练时间。
7. 表 2：B0—B3 的具体配置、选中 epoch、指标和计算开销。
8. 表 3：错误案例，包含问题、命中证据、参考答案、两种输出与错误类别。

截图按采集环节保存：环境配置 1 张；数据加载与划分 1 张；基础训练中间过程 1 张；基础评估 1 张；每次参数修改的启动参数与运行日志各留证据。日志需有 run_id、时间、配置和数据清单标识。截图保留完整命令窗口及上下文，避免仅截最后一行结果；不得后期伪造终端截图。

结果分析按四问组织：参数修改后发生什么变化；哪个设置在预定评价标准下更好；为什么可能出现这种现象；是否符合预期。区分事实指标和语言指标，不强求所有指标一致。若学习率过大未造成退化或嵌入未带来提升，也应保留真实结果并讨论原因。

## 八 实施顺序和提交

### 1 建议实施顺序

| 阶段 | 工作 | 通过条件 |
| --- | --- | --- |
| 第 1 阶段 | 用约 50 部电影构建试验数据，跑通实体链接、图查询和指标 | 三元组与参考答案一致；划分无电影交叉 |
| 第 2 阶段 | 完成正式抽取、清洗、QA 构建和人工核对 | 保存数据清单、哈希、规模统计和冻结划分 |
| 第 3 阶段 | 实现 A0、训练 TransE、搭建神经输入 | 能追溯每题证据；结构前缀确实进入模型 |
| 第 4 阶段 | 用小批数据做生成训练预检，冻结正式配置 | 损失有限、生成可解码、显存与时长可接受 |
| 第 5 阶段 | 完成 B0—B3 和 C0 | 独立重新训练，有配置、日志、checkpoint 和评估文件 |
| 第 6 阶段 | 鲁棒性和人工评估、绘图、报告与代码打包 | 所有结论可追溯到真实运行记录 |

建议预留约 5—7 个工作日，属于工作量预算，实际取决于数据可访问性、人工核对速度和硬件。正式训练前先测 1 个 epoch 的耗时再估算总时长，不预填未经测量的训练时间。此阶段尚未核实可用 GPU 型号和显存；CPU 可用于数据处理和管道系统，生成模型多次微调宜优先安排可用 GPU。

### 2 环境和项目内容

建议 Python 3.10 或 3.11，使用独立环境。主要依赖为 PyTorch、Transformers、SentencePiece、PyKEEN、scikit-learn、pandas、NumPy、SacreBLEU、rouge-score 和 Matplotlib。实施时核对兼容性，固定实际依赖版本与模型 revision，不把这里的建议写成已配置环境。

代码项目应包含数据获取与清洗、问答构建、实体链接、关系分类、TransE 训练、生成模型训练、统一评估、绘图、环境信息采集和批量实验配置。每次运行输出完整配置、训练曲线、指标汇总和逐题预测；逐题记录包含 question_id、预测实体、可见事实、参考答案、生成答案与耗时。

最终实验报告沿用教师原文的“二、实验基本信息”至“六、实验总结”标题和编号，其中“五、实验结果”完整保留 5.1—5.5 层级，依次记录基础设置与运行、基础结果、参数修改与重复、结果对比、对比分析。“一、总体要求”是提交规则，不应误写成学生实验内容。

代码压缩包只包含代码、配置、依赖说明、必要空目录／读取说明、查询与重建方法，不包含完整原始数据、处理后的完整问答集或大模型缓存。数据清单和哈希用于说明重建来源；因 Wikidata 会更新，未来在线重抓不保证逐字一致，应保留本地原始快照，并按课程允许的方式单独存档。提交前在干净环境实际验证可运行，不能仅提交算法原理或最终截图。

## 九 参考资料及用途

以下网页已于 2026 年 9 月 12 日打开核查，论文部分主要核对了出版页面的题目、摘要和元信息，尚不属于全文精读。

1. **Wikidata 官方数据库下载说明**。说明 JSON／RDF 导出、truthy 声明语义及 CC0 许可，支撑数据来源与图构建约定。https://www.wikidata.org/wiki/Wikidata:Database_download
2. **Wikidata 官方数据访问说明**。说明 SPARQL、小规模实体访问、修订号、缓存及限流处理，支撑本地子图获取方案。https://www.wikidata.org/wiki/Wikidata:Data_access
3. **Bordes et al. 2013. Translating Embeddings for Modeling Multi-relational Data. NeurIPS**。TransE 原始论文，解释以平移表示关系的思想。https://papers.nips.cc/paper/2013/hash/1cecc7a77928ca8133fa24680a88d2f9-Abstract.html
4. **PyKEEN TransE 官方文档**。确认实体／关系向量、嵌入维度和距离范数等实现接口。本文选择的参数是实验设计值，并非照抄全部库默认值。https://pykeen.readthedocs.io/en/stable/api/pykeen.models.TransE.html
5. **google/flan-t5-small 模型卡**。核查模型用途、加载方式、约 77M 参数规模和 Apache 2.0 许可；作为英文生成模型起点。https://huggingface.co/google/flan-t5-small
6. **Gardent et al. 2017. The WebNLG Challenge Generating Text from RDF Data. INLG**。提供三元组到自然语言生成及评价的相关研究背景；不替代 Wikidata 数据。https://aclanthology.org/W17-3518/
7. **SacreBLEU 官方仓库**。支持语料级 BLEU、多参考、固定 tokenizer、版本 signature 和可复现评分。https://github.com/mjpost/sacrebleu
8. **Lin. 2004. ROUGE A Package for Automatic Evaluation of Summaries**。ROUGE 原始资料，用于解释文本重叠评价与其适用边界。https://aclanthology.org/W04-1013/
9. **Trouillon et al. 2016. Complex Embeddings for Simple Link Prediction. ICML**。ComplEx 使用复数表示，能表达对称及反对称关系；可在进阶版与 TransE 比较，基础版不同时增加这一变量。https://proceedings.mlr.press/v48/trouillon16.html
10. **google/mt5-small 模型卡**。说明覆盖中文等多语言且需要下游微调，供中文问答版选型。https://huggingface.co/google/mt5-small

最值得先读的是 [1][2] 的数据访问部分、[3][4] 的 TransE 原理与实现、[5] 的模型卡和 [7] 的评测说明。执行阶段再阅读训练接口的对应版本文档，落实输入向量、attention mask、保存及加载等细节。
